/*
 * -------------------------------------------------
 *  Nextflow config file
 * -------------------------------------------------
 * Default config options for all environments.
 */

manifest {
  name = 'scEpigenome'
  description = 'Nextflow pipeline for scChIPseq inDrop data analysis'
  homePage = 'https://gitlab.curie.fr/sc-platform/scepigenome/-/tree/scCutTag-inDrop'
  author = 'P. Prompsy, Bioinformatics core facility'
  version = '1.0.0'
  mainScript = 'main.nf'
  nextflowVersion = '>=19.10.0'
  doi = ''
}

/**********
 * Params *
 **********/

params {
  email = false
  help = false
  design = false

  // Analysis parameters
  protocol = false 
  name = "${params.protocol}"
  genome = false
  reads = null
  readPaths = null
  samplePlan = false
  multiqcConfig = "$projectDir/assets/multiqcConfig.yaml"
  metadata = "$projectDir/assets/metadata.tsv"

  skipMultiQC = false
  skipSoftVersions = false
  skipBigWig = false

  // Output directories
  outDir = "$launchDir/results"
  summaryDir = "${params.outDir}/summary"
  tmpDir = './'

  //scChip
  darkCycleDesign = false
  // barcode + linker length to trim
  barcode_linker_length = 83
  //windowDup
  distDup = 50
  
  removeBlackRegions = true

  //Matrices
  minReadsPerCellmatrix = 100
  
  # Only for Mqc General Table : 
  minReadsPerCellmqcChIP = 1000
  minReadsPerCellmqcCUTindrop = 500
  minReadsPerCellmqcCUT10x = 100


  //bin sizes
  binSize = '50000,250' //ne marche plus si je mets que une valeur
  // tss gene window 10K so +/- 5000
  tssWindow = 5000

  macs2_sharp= "-p 0.001"
  max_feature_dist_sharp = 1000

  //sccut_10x
  barcodes10X_bwt2="${params.genomeAnnotationPath}/tools/10x/Barcodes_10X_bowtie2_737K-cratac-v1/bowtie_2_index/ref_index_1"
  barcode10X_start=1
  barcode10X_len=16

  barcodesIndrop {
    'indexB' {
      bwt2 = "${params.genomeAnnotationPath}/tools/scRNA_LBC_bowtie2_indexes/ref_index_1"
      start_nodarkcycles = 5
      start_darkcycles = 1
      size = 16
    }
    'indexC' {
      bwt2 = "${params.genomeAnnotationPath}/tools/scRNA_LBC_bowtie2_indexes/ref_index_2"
      start_nodarkcycles = 25
      start_darkcycles = 21
      size = 16
    }
    'indexD' {  
      bwt2 = "${params.genomeAnnotationPath}/tools/scRNA_LBC_bowtie2_indexes/ref_index_3"
      start_nodarkcycles  = 45
      start_darkcycles = 41
      size = 16
    }
  }
}

/************************
 * Configs and profiles *
 ************************/

// Additional configs
includeConfig 'conf/base.config'
includeConfig 'conf/process.config'
includeConfig 'conf/geniac.config'

// Profiles
profiles {
  conda {
    includeConfig 'conf/conda.config'
  }
  multiconda {
    includeConfig 'conf/multiconda.config'
  }
  docker {
    includeConfig 'conf/docker.config'
  }
  singularity {
    includeConfig 'conf/singularity.config'
  }
  path {
    includeConfig 'conf/path.config'
  }
  multipath {
    includeConfig 'conf/multipath.config'
  }
  podman {
    includeConfig 'conf/podman.config'
  }
  cluster {
    includeConfig 'conf/cluster.config'
  }
  test{
    includeConfig 'conf/test.config'
  }
}

// Annotation paths
includeConfig 'conf/genomes.config'

// Load modules
includeConfig 'conf/modules.config'
